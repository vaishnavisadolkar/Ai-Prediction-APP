"""
Diabetes prediction ML service (core package).

This package holds training and inference logic shared by the FastAPI app
under ``api/``. Install with ``pip install -e .`` from the ``ml-service``
directory so imports like ``from ml_service.predict import run_prediction`` work.
"""
